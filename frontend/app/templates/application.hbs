<div class="menu">
  {{#if this.session.isAuthenticated}}
  <a {{on "click" this.invalidateSession}}>Logout</a>
  {{else}}
  <LinkTo @route="login" />
  {{/if}}
</div>
<div class="main">
  {{outlet}}
</div>